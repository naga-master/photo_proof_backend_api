# Photo Proof API Configuration
# Generated by quickstart.py

# Database - PostgreSQL Only
DATABASE_URL=postgresql://photo_proof_user:PhotoProof2024!@localhost/photo_proof_production

# Security (CHANGE IN PRODUCTION!)
SECRET_KEY=HX3-m-WYoZibjEgD_lHO_INTPlX_4alt9p9g33Z10AI
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Storage
USE_S3=false
UPLOAD_DIR=/Users/ns632@apac.comcast.com/Documents/v0_photo_proof/photo_proof_api/uploads
MAX_UPLOAD_SIZE=10485760

# CORS - Explicit origins for reliable CORS handling (wildcards don't work with regex)
CORS_ORIGINS=http://localhost:3001,http://localhost:5173,http://localhost:3000,http://localhost:8000,http://localhost:8081,http://127.0.0.1:3001,http://127.0.0.1:5173,http://127.0.0.1:3000,http://127.0.0.1:8000

# Environment
ENVIRONMENT=development
APP_ENV=development

# Cookie Security Settings
# Set to 'true' in production with HTTPS
COOKIE_SECURE=false
# Options: 'strict', 'lax', 'none' (use 'strict' in production)
COOKIE_SAMESITE=lax
# Leave empty for current domain only, use '.domain.com' for subdomains in production
# COOKIE_DOMAIN=
